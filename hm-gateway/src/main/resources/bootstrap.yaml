spring:
  application:
    name: gateway  # 微服务名称
  profiles:
    active: local
  cloud:
    nacos:
      server-addr: 192.168.89.128:8848
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-log.yaml


